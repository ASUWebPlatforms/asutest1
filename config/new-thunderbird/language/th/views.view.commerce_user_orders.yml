display:
  default:
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      pager:
        options:
          tags:
            first: '« first'
            last: 'last »'
      fields:
        order_id:
          label: ไอดี
        placed:
          label: วันที่
      arguments:
        uid:
          exception:
            title: ทั้งหมด
