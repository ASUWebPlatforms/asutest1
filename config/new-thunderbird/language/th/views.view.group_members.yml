display:
  default:
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      pager:
        options:
          tags:
            first: '« first'
            last: 'last »'
      fields:
        group_roles:
          label: Roles
        dropbutton:
          label: Operations
      arguments:
        gid:
          exception:
            title: ทั้งหมด
