{% set classes = [ 
  'linkcollection',  
  'block', 
  'block-' ~ configuration.provider|clean_class, 
  'block-' ~ plugin_id|clean_class
  ] %}
<div {{ attributes.addClass(classes) }}>
    <!--TODO: Replace collection content -->
    PLACEHOLDER FOR DOWNLOADS (LINK COLLECTION)
    {{ content }}
 </div>