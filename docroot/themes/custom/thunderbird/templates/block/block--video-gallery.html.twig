{# Assign column value #}
{% set columns = content.field_columns['#items'].getString() %}
{% if columns is empty %}{% set columns = 'four-columns' %}{% endif %}

{# Construct block classes #}
{% set classes = [
  'video-gallery',
  'container',
  columns
] %}

{# Render block #}
{% extends 'block.html.twig' %}
{% block content %}
  {{ attach_library('thunderbird/lightbox') }}
  {{ content.field_video }}
{% endblock %}